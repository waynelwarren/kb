all: .gitconfig vimrc.local .tmux.conf pathlist

.gitconfig: ~/.gitconfig
	cp ~/.gitconfig .

.tmux.conf: ~/.tmux.conf
	cp ~/.tmux.conf .

.vimrc: ~/.vimrc
	cp ~/.vimrc .

pathlist: ~/bin/pathlist
	cp ~/bin/pathlist .
